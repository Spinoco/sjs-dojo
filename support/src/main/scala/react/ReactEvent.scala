package react

/**
 * An Event, that is dispatched by `Children` components.
 * This event is always consumed by any parent node.
 * If the root node publishes this event, then it is ignored.
 */
trait ReactEvent
